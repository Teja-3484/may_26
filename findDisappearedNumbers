class Solution:
    def findDisappearedNumbers(self, nums: List[int]) -> List[int]:
        n = len(nums)
        d = {}
        ans = []

        for i in nums:
            d[i] = 1

        for i in range(1, n + 1):
            if i not in d:
                ans.append(i)

        return ans
